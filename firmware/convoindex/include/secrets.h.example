// Copy this file to secrets.h and fill in your values. secrets.h is gitignored.
#pragma once

#define WIFI_SSID       "your-wifi-ssid"
#define WIFI_PASSWORD   "your-wifi-password"

// IP address (or hostname) of the machine running local-backend/capture_server.py
#define CAPTURE_SERVER_HOST "192.168.1.100"
#define CAPTURE_SERVER_PORT 8005
